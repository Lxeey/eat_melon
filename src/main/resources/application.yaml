server:
  port: 9000
spring:
  application:
    name: melon
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/melon?serverTimezone=UTC&&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver

